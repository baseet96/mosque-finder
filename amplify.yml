version: 1
frontend:
  phases:
    preBuild:
      commands:
        # - npm ci --cache .npm --prefer-offline
        - npm install --include=dev --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths: []
      # - .npm/**/*
